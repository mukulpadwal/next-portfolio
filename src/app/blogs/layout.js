export const metadata = {
  title: "Mukul Padwal | Blogs",
  description: "Portfolio Website",
  keywords: ["mukulpadwal", "mukul", "padwal"],
};

export default function BlogsPageLayout({ children }) {
  return <section>{children}</section>;
}
