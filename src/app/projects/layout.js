export const metadata = {
  title: "Mukul Padwal | Projects",
  description: "Portfolio Website",
  keywords: ["mukulpadwal", "mukul", "padwal"],
};

export default function ProjectsPageLayout({ children }) {
  return (
    <section>
      {children}
    </section>
  );
}